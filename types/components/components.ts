/**
 * Represents the properties of an Icon component.
 */
interface IconProps {
  className?: string;
  color?: string;
  size?: string;
}

interface EditContentObjectProps {
  id: string;
  write_token: string;
  type: string;
  qlib_id: string;
  writeToken: string;
  objectId: string;
  nodeUrl: string;
}
